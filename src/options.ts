import { CompType } from "@connectv/html";

export interface Options<R, T> {
  Space: CompType<R, T>,
  Hr: CompType<R, T>,
  Heading: CompType<R, T>,
  // Code: CompType<R, T>,
  // Table: CompType<R, T>,
  // TableRow: CompType<R, T>,
  // TableCell: CompType<R, T>,
  // BlockQuote: CompType<R, T>,
  // List: CompType<R, T>,
  // ListItem: CompType<R, T>,
  // Html: CompType<R, T>,
  // Paragraph: CompType<R, T>,
  // Text: CompType<R, T>,
}
